<!--<p id="notice"><%= notice %></p>-->
<div>
<h1>Cursadas</h1>

<table class="table table-bordered">
  <thead>
    <tr>
      <th class="bg-info text-white">Año</th>
      <th class="bg-info text-white">Titulo</th>
      <th colspan="3" class="bg-info text-white"></th>
    </tr>
  </thead>

  <tbody>
    <% @courses.each do |course| %>
      <tr>
        <td><%= course.year %></td>
        <td><%= course.title %></td>
        <td><%= link_to 'Mostrar', course %></td>
        <td><%= link_to 'Editar', edit_course_path(course) %></td>
        <td><%= link_to 'Eliminar', course, method: :delete, data: { confirm: 'Estas seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<div align="center">
<%= link_to 'Crear nueva cursada', new_course_path%>
</div>