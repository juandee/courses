<p id="notice"><%= notice %></p>

<h1>Pupils</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Surname</th>
      <th>Legajo</th>
      <th>Dni</th>
      <th>Email</th>
      <th>Course</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @pupils.each do |pupil| %>
      <tr>
        <td><%= pupil.name %></td>
        <td><%= pupil.surname %></td>
        <td><%= pupil.legajo %></td>
        <td><%= pupil.dni %></td>
        <td><%= pupil.email %></td>
        <td><%= pupil.course.title %></td>
        <td><%= link_to 'Show', course_pupil_path(@course,pupil) %></td>
        <td><%= link_to 'Edit', edit_course_pupil_path(@course,pupil) %></td>
        <td><%= link_to 'Destroy', course_pupil_path(@course,pupil), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Pupil', new_course_pupil_path %>
